<%= render "project_header" %>

<div class="container">
  <div class="album text-muted">
    <%= paginate @projects %>
      <div class="row sortable">
        <%= render @projects %>
      </div>
    <%= paginate @projects %>
  </div>
</div>
