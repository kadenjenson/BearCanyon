<%= render "shared/project_header" %>

<div class="container">
  <div class="album text-muted">
    <%= paginate @projects %>
      <div class="row">
        <%= render @projects %>
      </div>
    <%= paginate @projects %>
  </div>
</div>
